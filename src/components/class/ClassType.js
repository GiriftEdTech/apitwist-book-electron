const ClassType = ({ title }) => {
    return (
        <div className="class_carousel_header">
            <h1 className="class_carousel_title">{title}</h1>
        </div>
    )
}

export default ClassType
